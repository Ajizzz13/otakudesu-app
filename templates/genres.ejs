<%- include('partials/header') %>

<div class="rail-head">
  <h1 class="rail-title">Genre</h1>
</div>

<div class="genre-chips big">
  <% genres.forEach((g) => { %>
    <a class="chip" href="/genres/<%= g.slug %>"><%= g.name %></a>
  <% }) %>
</div>

<%- include('partials/footer') %>